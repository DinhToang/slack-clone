"use clent";//this is a react component not a react server component, can use reactivity and interactivity inside it: use state or on click 

export const AuthScreen = () =>{
    return(
        <div>
            Auth screen
        </div>
    )
}